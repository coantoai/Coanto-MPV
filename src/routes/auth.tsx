import{createFileRoute,useNavigate}from"@tanstack/react-router";import{FormEvent,useEffect,useState}from"react";import{supabase}from"@/integrations/supabase/client";function AuthPage(){const nav=useNavigate();const[signup,setSignup]=useState(false),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);useEffect(()=>{void supabase.auth.getSession().then(({data})=>{if(data.session)void nav({to:"/"})})},[nav]);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{if(signup){const{data,error}=await supabase.auth.signUp({email:email.trim(),password,options:{emailRedirectTo:`${window.location.origin}/`}});if(error)throw error;if(!data.session){setError("تم إنشاء الحساب. تحقق من بريدك ثم سجّل الدخول.");return}}else{const{error}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(error)throw error}await nav({to:"/"})}catch(e){setError(e instanceof Error?e.message:"تعذّر إتمام العملية")}finally{setBusy(false)}}return <div dir="rtl" className="grid min-h-screen place-items-center bg-[#060910] p-4 text-white"><form onSubmit={submit} className="w-full max-w-md space-y-3 rounded-3xl border border-[#223148] bg-[#0d141f] p-6"><div className="text-xl font-black tracking-[3px] text-[#29d3bd]">COANTO</div><h1 className="text-2xl font-black">{signup?"إنشاء حساب":"تسجيل الدخول"}</h1><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="البريد الإلكتروني" className="w-full rounded-xl border border-white/10 bg-[#090e16] px-4 py-3"/><input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)} placeholder="كلمة المرور" className="w-full rounded-xl border border-white/10 bg-[#090e16] px-4 py-3"/><button disabled={busy} className="w-full rounded-xl bg-[#29d3bd] py-3 font-black text-[#06100e]">{busy?"جارٍ...":signup?"إنشاء الحساب":"دخول"}</button>{error&&<div className="rounded-xl border border-amber-300/30 p-3 text-xs text-amber-200">{error}</div>}<button type="button" onClick={()=>setSignup(!signup)} className="w-full text-xs text-[#91a2b8]">{signup?"لديك حساب؟ سجّل الدخول":"ليس لديك حساب؟ أنشئ حسابًا"}</button></form></div>}
// @ts-expect-error TanStack file-route type map is generated without declarations in this project template.
export const Route=createFileRoute("/auth")({component:AuthPage});
