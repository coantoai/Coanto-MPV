import{createClient}from"@supabase/supabase-js";import type{Database}from"./types";function make(){const url=process.env["SUPABASE_URL"],key=process.env["SUPABASE_SERVICE_ROLE_KEY"];if(!url||!key)throw new Error("Missing Supabase server environment variables");return createClient<Database>(url,key,{auth:{persistSession:false,autoRefreshToken:false}})}let c:ReturnType<typeof make>|undefined;export const supabaseAdmin=new Proxy({} as ReturnType<typeof make>,{get(_,p,r){if(!c)c=make();return Reflect.get(c,p,r)}});
