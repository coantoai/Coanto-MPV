import{createClient}from"@supabase/supabase-js";import type{Database}from"@/integrations/supabase/types";export async function getUserIdFromRequest(request:Request){const h=request.headers.get("authorization");if(!h?.startsWith("Bearer "))return null;const token=h.slice(7).trim();if(!token)return null;const url=process.env["SUPABASE_URL"],key=process.env["SUPABASE_PUBLISHABLE_KEY"];if(!url||!key)return null;const c=createClient<Database>(url,key,{global:{headers:{Authorization:`Bearer ${token}`}},auth:{persistSession:false,autoRefreshToken:false,storage:undefined}});const{data,error}=await c.auth.getClaims(token);return error||!data?.claims?.sub?null:String(data.claims.sub)}
