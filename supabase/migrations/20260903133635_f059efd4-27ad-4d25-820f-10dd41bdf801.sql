CREATE TABLE public.analyses(id UUID NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY,user_id UUID,store_url TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT now(),result_json JSONB NOT NULL);CREATE INDEX analyses_created_at_idx ON public.analyses(created_at DESC);GRANT ALL ON public.analyses TO service_role;ALTER TABLE public.analyses ENABLE ROW LEVEL SECURITY;
